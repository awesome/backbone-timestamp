{
  "scripts": {
    "postinstall": "bower install",
    "test": "testem -f test/testem.json ci",
    "start": "testem -f test/testem.json",
    "stat": "cloc index.js --quiet && cloc test/ --quiet"
  },
  "devDependencies": {
    "testem": "~0.3.x",
    "bower": "~1.0.x"
  }
}
